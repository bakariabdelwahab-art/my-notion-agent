# 🎨 وكيل إنشاء قوالب Notion

تطبيق ويب يستخدم الذكاء الاصطناعي لتحويل الوصف النصي إلى قوالب Notion حقيقية بشكل تلقائي.

## ✨ الميزات

- **واجهة رسومية سهلة:** لا حاجة لسطر الأوامر، فقط أدخل الوصف واضغط على الزر.
- **ذكاء اصطناعي متقدم:** يستخدم OpenAI لفهم الوصف وتحويله إلى قالب احترافي.
- **تكامل مباشر مع Notion:** ينشئ القالب مباشرة في قاعدة البيانات الخاصة بك.
- **دعم اللغة العربية:** واجهة وتعليمات بالعربية بالكامل.

## 📋 المتطلبات

قبل البدء، تأكد من أن لديك:

1. **حساب Notion:** اذهب إلى [notion.so](https://www.notion.so) وأنشئ حساباً.
2. **رمز التكامل (Integration Token):** 
   - اذهب إلى [My Integrations](https://www.notion.so/my-integrations)
   - أنشئ تكامل جديد وانسخ الرمز (يبدأ بـ `ntn_...`)
3. **معرف قاعدة البيانات (Database ID):**
   - أنشئ قاعدة بيانات جديدة في Notion
   - انسخ معرفها من الرابط (32 حرفاً ورقماً)
   - **مهم:** شارك قاعدة البيانات مع التكامل الذي أنشأته
4. **Python 3.8+:** تثبيت Python على جهازك

## 🚀 التثبيت والتشغيل المحلي

### الطريقة 1: التشغيل على جهازك

1. **استنساخ أو تنزيل الملفات:**
   ```bash
   # إذا كنت تستخدم Git
   git clone <رابط المستودع>
   cd notion-template-agent
   ```

2. **تثبيت المكتبات:**
   ```bash
   pip install -r requirements.txt
   ```

3. **تشغيل التطبيق:**
   ```bash
   streamlit run notion_template_app.py
   ```

4. **فتح التطبيق:**
   - سيفتح تلقائياً في متصفحك على `http://localhost:8501`

### الطريقة 2: النشر على الإنترنت (مجاني)

#### استخدام Streamlit Cloud (الأسهل)

1. **إنشاء حساب GitHub:**
   - اذهب إلى [github.com](https://github.com) وأنشئ حساباً مجاناً

2. **رفع الملفات على GitHub:**
   - أنشئ مستودع جديد (Repository)
   - ارفع الملفات التالية:
     - `notion_template_app.py`
     - `requirements.txt`
     - `README.md` (هذا الملف)

3. **نشر على Streamlit Cloud:**
   - اذهب إلى [share.streamlit.io](https://share.streamlit.io)
   - سجل دخولك باستخدام حساب GitHub
   - انقر على "New app"
   - اختر المستودع والملف الرئيسي (`notion_template_app.py`)
   - انقر على "Deploy"

4. **الحصول على الرابط:**
   - سيحصل تطبيقك على رابط عام (مثل: `https://your-app-name.streamlit.app`)
   - يمكنك الآن الوصول إليه من أي جهاز!

#### استخدام Hugging Face Spaces (بديل)

1. **إنشاء حساب Hugging Face:**
   - اذهب إلى [huggingface.co](https://huggingface.co) وأنشئ حساباً

2. **إنشاء Space جديد:**
   - اختر "Streamlit" كنوع التطبيق
   - ارفع الملفات الثلاثة

3. **الانتظار والاستخدام:**
   - سيتم نشر التطبيق تلقائياً

## 📖 كيفية الاستخدام

1. **فتح التطبيق:**
   - إذا كنت تشغله محلياً: `http://localhost:8501`
   - إذا كنت تستخدم Streamlit Cloud: الرابط الذي حصلت عليه

2. **إدخال البيانات:**
   - في الشريط الجانبي، أدخل:
     - **رمز التكامل:** (نسخه من My Integrations)
     - **معرف قاعدة البيانات:** (معرف قاعدة البيانات التي ستحتوي على القوالب)

3. **كتابة الوصف:**
   - في المربع الرئيسي، اكتب وصفاً تفصيلياً للقالب الذي تريده
   - **مثال:**
     ```
     أريد قالبًا لإدارة المشاريع يحتوي على:
     - عنوان رئيسي "خطة المشروع"
     - قسم "المهام" بقائمة مهام فارغة
     - قسم "الملاحظات" في كتلة تبديل
     - قسم "الموارد" بقائمة نقطية
     ```

4. **الضغط على "إنشاء القالب":**
   - سيقوم التطبيق بإنشاء القالب في Notion
   - سيعرض لك الرابط المباشر للقالب

5. **المشاركة (خطوة يدوية):**
   - افتح الرابط في Notion
   - انقر على "Share" في الزاوية العلوية اليمنى
   - فعّل "Share to web" للحصول على رابط قابل للمشاركة

## ⚙️ متغيرات البيئة (للنشر المتقدم)

إذا كنت تريد تخزين بيانات الاتصال بشكل آمن:

1. **في Streamlit Cloud:**
   - اذهب إلى إعدادات التطبيق
   - أضف "Secrets" جديدة:
     ```
     NOTION_TOKEN = "ntn_..."
     NOTION_PARENT_ID = "c4d14bc60529487fa7a6a85c7539d4e1"
     OPENAI_API_KEY = "sk-..."
     ```

2. **في الكود:**
   - عدّل `notion_template_app.py` لاستخدام:
     ```python
     notion_token = st.secrets.get("NOTION_TOKEN", "")
     parent_db_id = st.secrets.get("NOTION_PARENT_ID", "")
     ```

## 🐛 استكشاف الأخطاء

### خطأ: "Couldn't find database with ID"
- **السبب:** قاعدة البيانات لم تتم مشاركتها مع التكامل
- **الحل:** اذهب إلى قاعدة البيانات، انقر على "Share"، وأضف التكامل

### خطأ: "Invalid API key"
- **السبب:** رمز التكامل غير صحيح
- **الحل:** تحقق من الرمز في My Integrations

### خطأ: "OpenAI API error"
- **السبب:** مشكلة في الاتصال بـ OpenAI
- **الحل:** تأكد من أن مفتاح OpenAI API صحيح ولديك رصيد

## 📚 أمثلة على الأوصاف

### مثال 1: قالب تدوين الملاحظات
```
أريد قالبًا لتدوين الملاحظات اليومية يحتوي على:
- عنوان رئيسي "ملاحظات اليوم"
- قسم "الأفكار الرئيسية" في كتلة تبديل
- قسم "المهام" بقائمة مهام فارغة
- قسم "الملاحظات الإضافية" بفقرة عادية
```

### مثال 2: قالب إدارة المشاريع
```
أريد قالبًا لإدارة المشاريع يحتوي على:
- عنوان رئيسي "تفاصيل المشروع"
- قسم "الفريق" بقائمة نقطية
- قسم "المراحل" بقائمة مهام
- قسم "الموارد المطلوبة" في كتلة تنبيه زرقاء
```

### مثال 3: قالب خطة المحتوى
```
أريد قالبًا لخطة المحتوى يحتوي على:
- عنوان رئيسي "خطة المحتوى الشهرية"
- قسم "أفكار جديدة" في كتلة تبديل
- قسم "محتوى قيد التنفيذ" بقائمة مهام
- قسم "محتوى منشور" بقائمة نقطية
- قسم "ملاحظات" في كتلة تنبيه صفراء
```

## 🔐 الأمان

- **لا تشارك رموزك:** لا تنشر رمز التكامل أو مفتاح OpenAI علناً
- **استخدم Secrets:** عند النشر على الإنترنت، استخدم متغيرات البيئة (Secrets)
- **تحديثات منتظمة:** احرص على تحديث المكتبات بانتظام

## 📞 الدعم والمساعدة

إذا واجهت مشكلة:

1. تحقق من قسم "استكشاف الأخطاء" أعلاه
2. تأكد من أن جميع البيانات صحيحة
3. تحقق من أن قاعدة البيانات تمت مشاركتها مع التكامل

## 📝 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الشخصي والتجاري.

## 🙏 شكر خاص

- Notion لـ API المتقدمة
- OpenAI لـ نموذج GPT
- Streamlit لـ منصة التطوير السهلة

---

**استمتع بإنشاء قوالب Notion بسهولة! 🚀**
